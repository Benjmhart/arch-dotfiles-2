# .xinitrc runs `setxkbmap -option caps:escape` (task 27). Left at the default,
# fcitx5 reapplies its own XKB config on input-method switch and silently drops
# that option, so Caps Lock would stop acting as Escape after typing Japanese.
Allow Overriding System XKB Settings=False
